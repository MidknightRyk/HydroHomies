# exclude the following directories
node_modules/
api/uploads/
uploads/

# The following are no deployed for security purposes
.env

# Don't track these files, used for heroku deployment
Procfile

# Packages #
############
# it's better to unpack these files and commit the raw source
# git has its own built in compression methods
*.7z
*.dmg
*.gz
*.iso
*.jar
*.rar
*.tar
*.zip
*.idea
